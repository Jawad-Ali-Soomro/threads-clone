import Sidebar from "../_components/Sidebar";

const Home = () => {
  // const infoToParse = window.localStorage.getItem("googleUserInfo");
  // const userInfo = JSON.parse(infoToParse);
  return (
    <div>
      <Sidebar />
    </div>
  );
};

export default Home;
